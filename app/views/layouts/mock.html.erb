<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Splorf Coach</title>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
        <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
        <%= csrf_meta_tags %>
        <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
        <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
        <!--[if lt IE 9]>
          <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
          <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
        <![endif]-->
    </head>
    <body>
        <div class="navbar navbar-default navbar-fixed-top my-custom-nav" role="navigation">
            <a class="navbar-brand" href="#">
            <%#TODO do I want to support browsers that don't use svg?%>
            <%= image_tag "splorf_head.svg", 
                          height: 50,
                          alt: "splorf head logo and home link",
                          id: "brand_logo" =%>
            </a>
            <div class="container">
                <button type="button"
                        class="navbar-toggle"
                        data-toggle="collapse"
                        data-target=".navbar-collapse">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <div class="navbar-text cursor bucket-dropdown-head"
                     data-toggle="dropdown"
                     data-target=".bucket-dropdown">
                    <span id="bucket-dropdown-head-text">New Stuff</span>
                    <b class="caret"></b>
                </div>         
                <div class="collapse navbar-collapse">
                    <ul class="nav navbar-nav navbar-right">
                        <li class="navbar-text">Signed in as user@userdomain.com</li>
                        <li><a href="#">logout</a></li>
                    </ul>
                </div>
                <ul class="nav navbar-nav">
                    <li class="dropdown bucket-dropdown">
                    <ul class="dropdown-menu">
                        <% bucket_names = ['Waiting', 'Ready', 'WIP', 'Done', 'Remember', 'People']
                           bucket_names.each do |bucket_name| %>
                            <li><%= link_to bucket_name, '#'%></li>
                        <% end %>
                    </ul>
                    </li>
                </ul>
            </div>
        </div>

        <div id="plus-button-group" class="btn-group btn-group-justified btn-group-lg">
            <% ['Thing', 'Habit', 'Person'].each do |button_label| %>
                <div class="btn-group">
                    <button type="button" class="btn btn-default plus-button">
                        +<%= button_label %>
                    </button>
                </div>
            <% end %>
        </div>

        <ul id="sortable-bucket-item-list" class="list-unstyled">
            <% 3.times do |i|%>
                <li class="panel panel-info">
                    <div class="panel-heading">
                        <h4 class="panel-title">
                            <a data-toggle="collapse"
                              data-parent="#accordion"
                              href="#collapse<%= i + 1 %>">
                                Thing: description of thing
                            </a>
                        </h4>
                    </div>
                    <div id="collapse<%= i + 1 %>" class="panel-collapse collapse">
                        <div class="panel-body">Content <%= i + 1 %></div>
                    </div>
                </li>
            <% end %>
        </ul>
    </body>
</html>
